#include <stdio.h>
#include <stdlib.h>
/* int printfabb()
{
    int first = 0, secound = 1, output;
    printf("%d ", first);printf("%d ", secound);
    for (int i = 0; i < 23; i++)
    {
        output = first + secound;
        printf("%d ", output);
        int temp = first;
        first = secound;
        secound = output;
    }
} */
//recursion
int printfabb()
{
    num = 0;
    if(num <= 0)
    {
        return 0;
    }
    
}
int main()
{
    printfabb();
    return 0;
}