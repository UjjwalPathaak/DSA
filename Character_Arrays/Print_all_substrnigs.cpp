#include<bits/stdc++.h>
using namespace std;
void subString(string input, int size)
{
    int start = 0;
    for (int i = 0; i < size; i++)
    {
        for (int j = 0; j < size; j++)
        {
            cout << 
        }
        
    }
    
}
int main()
{
    string input; 
    cin >> input;
    int size = input.length();
    subString(input, size);

return 0;
}